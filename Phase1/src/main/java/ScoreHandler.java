/**
 * The ScoreHandler interface represents a component that handles scoring the documents.
 */
public interface ScoreHandler {

    /**
     * Gives a score to a document.
     */
    void giveScore();
}
